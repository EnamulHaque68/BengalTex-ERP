{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "//ConnectionStrings": "Set via env vars in production: ConnectionStrings__DefaultConnection and ConnectionStrings__HangfireConnection (use double-underscore to nest).",

  "//Jwt": "Set the actual signing key via env var Jwt__Secret (32+ bytes random). Issuer/Audience may be overridden via Jwt__Issuer / Jwt__Audience.",
  "Jwt": {
    "Issuer": "BengalTexERP",
    "Audience": "BengalTexERP.Api",
    "AccessTokenExpiresInMinutes": 15,
    "RefreshTokenExpiresInDays": 7
  },

  "//DeviceFingerprint": "Set DeviceFingerprint__Salt via env var (32+ bytes random).",

  "//Cors": "Set Cors__AllowedOrigins__0 (and __1, __2, ...) via env vars to your actual SPA origins. NEVER use * for AllowedOrigins in production with credentials.",

  "Application": {
    "FrontendBaseUrl": "https://CHANGE-ME-FRONTEND-URL",
    "BaseCurrency": "BDT",
    "DefaultGeoFenceRadiusMeters": 500,
    "MaxFailedLoginAttempts": 5,
    "LockoutDurationMinutes": 15
  },

  "FileStorage": {
    "RootPath": "/data/uploads"
  },

  "Approvals": {
    "PurchaseOrderThreshold": 50000,
    "PurchaseOrderApproverRole": "Admin"
  },

  "Sms": {
    "//Provider": "Set to Twilio / Vonage / etc. in production. DevLogger writes to logs only.",
    "Provider": "DevLogger"
  },

  "Email": {
    "//Provider": "Set to Smtp / SendGrid / etc. in production. DevLogger writes to logs only.",
    "Provider": "DevLogger",
    "FromAddress": "noreply@CHANGE-ME-DOMAIN",
    "FromName": "Bengal TEX ERP"
  },

  "//Database": "Set Database__InitializeOnStartup=true to auto-apply EF migrations + run idempotent seeder on container boot. Recommended for fresh deploys; toggle off once stable.",

  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft": "Warning",
        "System": "Warning",
        "Microsoft.EntityFrameworkCore": "Warning",
        "Microsoft.AspNetCore.RateLimiting": "Information"
      }
    },
    "WriteTo": [
      { "Name": "Console" },
      {
        "Name": "File",
        "Args": {
          "path": "/var/log/bengaltex/log-.txt",
          "rollingInterval": "Day",
          "retainedFileCountLimit": 90,
          "outputTemplate": "{Timestamp:yyyy-MM-dd HH:mm:ss.fff zzz} [{Level:u3}] {SourceContext} {Message:lj}{NewLine}{Exception}"
        }
      }
    ],
    "Enrich": [
      "FromLogContext",
      "WithMachineName",
      "WithThreadId",
      "WithProcessId"
    ]
  }
}
